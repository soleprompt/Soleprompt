/**
 * Set a user's role to admin directly in the database:
 *
 *   npx prisma studio
 *
 * Open the User table, find the user by email, and set role to "admin".
 *
 * Or via SQL:
 *
 *   UPDATE User SET role = 'admin' WHERE email = 'you@example.com';
 */

export {};
